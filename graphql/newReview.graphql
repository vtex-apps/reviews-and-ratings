mutation NewReview($review: ReviewInput!) {
    newReview(review: $review) {
        id
        cacheId
        productId
        rating
        title
        text
        reviewerName
        shopperId
        reviewDateTime
        verifiedPurchaser
		sku
    }
}