mutation NewReview($review: ReviewInput!) {
  newReview(review: $review) {
    id
    cacheId
    productId
    rating
    title
    text
    reviewerName
    shopperId
    location
    reviewDateTime
    verifiedPurchaser
    sku
  }
}
