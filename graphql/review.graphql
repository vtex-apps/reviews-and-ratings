query Review($id: ID!) {
  review(id: $id) {
    id
    productId
    rating
    title
    text
    reviewerName
    location
    locale
    shopperId
    reviewDateTime
    verifiedPurchaser
    sku
    approved
  }
}
