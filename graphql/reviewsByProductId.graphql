query ReviewsByProductId(
  $productId: String!
  $from: Int
  $to: Int
  $orderBy: String
  $status: String
) {
  reviewsByProductId(
    productId: $productId
    from: $from
    to: $to
    orderBy: $orderBy
    status: $status
  ) {
    data {
      id
      productId
      rating
      title
      text
      reviewerName
      location
      reviewDateTime
      verifiedPurchaser
      sku
      approved
    }
    range {
      total
      from
      to
    }
  }
}
