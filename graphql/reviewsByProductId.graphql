query ReviewsByProductId(
  $productId: String!
  $rating: Int
  $from: Int
  $to: Int
  $orderBy: String
  $status: String
) {
  reviewsByProductId(
    productId: $productId
    rating: $rating
    from: $from
    to: $to
    orderBy: $orderBy
    status: $status
  ) {
    data {
      id
      productId
      rating
      title
      text
      reviewerName
      location
      reviewDateTime
      verifiedPurchaser
      sku
      approved
    }
    range {
      total
      from
      to
    }
  }
}
