query Reviews($offset: Int, $limit: Int, $orderBy: String) {
  reviews(offset: $offset, limit: $limit, orderBy: $orderBy) {
    id
    cacheId
    productId
    rating
    title
    text
    reviewerName
    location
    shopperId
    reviewDateTime
    verifiedPurchaser
    sku
    approved
  }
}
