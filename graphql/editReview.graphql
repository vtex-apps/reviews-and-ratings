mutation EditReview($id: String!, $review: EditReviewInput!) {
  editReview(id: $id, review: $review) {
    id
    productId
    rating
    title
    text
    reviewerName
    location
    shopperId
    reviewDateTime
    verifiedPurchaser
    sku
  }
}
