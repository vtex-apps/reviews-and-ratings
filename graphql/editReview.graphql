mutation EditReview($id: ID!, $review: ReviewInput!) {
  editReview(id: $id, review: $review) {
    id
    cacheId
    productId
    rating
    title
    text
    reviewerName
    location
    shopperId
    reviewDateTime
    verifiedPurchaser
    sku
  }
}
