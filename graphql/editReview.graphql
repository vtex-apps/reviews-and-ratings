mutation EditReview($id: String!, $review: ReviewInput!) {
  editReview(id: $id, review: $review) {
    id
    productId
    rating
    title
    text
    reviewerName
    location
    shopperId
    reviewDateTime
    verifiedPurchaser
    sku
  }
}
